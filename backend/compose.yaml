services:
  postgres:
    image: 'postgres:16-alpine'
    environment:
      - 'POSTGRES_DB=pos_db'
      - 'POSTGRES_PASSWORD=phaiecobyte'
      - 'POSTGRES_USER=phaiecobyte'
    ports:
      - '5433:5432'
    volumes:
      - pos_data:/var/lib/postgresql/data

volumes:
  pos_data:
